# 学生工作档案袋与本轮修复

## 实现依据

Figma `I0vjlstlIsfglzKtYf6oK3` 的 `33:11` / `33:17` 为展开状态参考，通过 Figma MCP 读取布局和下载原素材。新增文件位于 `assets/about/student-work/`，五张照片的四个原图、收回箭头、牛皮纸参考均已成功下载，无无法读取的资产。牛皮纸继续使用 About 原有透明素材，保留收起状态的原始外观；仪仗队两张照片按 Figma 自身的同一原图裁切参数显示。

`scripts/student-work.js`、`styles/student-work.css` 独立管理 closed / hover / opening / open / closing。没有替换现有 About 内容；“阅览我的项目”的原 DOM、位置、文案、CSS、点击处理及路由均保留。

## 位置与时间

- 源画布 1568×900，牛皮纸宽 680.376、高 699；局部坐标从 `(-36,108)` 到 `(433,96)`，随原 About composition 的 24px 偏移，展开实际源画布坐标为 `(457,96)`。
- 收起时只有牛皮纸真实露出的不透明区域接受鼠标或触摸。蓝卡、周围透明区域不会触发；键盘也可打开。
- Hover 右移 6px、上移 6px、放大至 1.02 并略增强投影，显示“点击阅览学生工作”；无声音。
- 展开移动 720ms，在 25% 即 180ms 才提升到蓝卡之上；略微超调后停稳。右侧履历文字 420ms 降至 39%，蓝卡、照片、联系方式和项目按钮保持原透明度。
- 六组内容在 360ms 起按 70ms 间隔出现，每组 280ms，总展开 990ms。
- 收回先反序淡出内容，300ms 后用 680ms 收回；移动完成 75% 时降低图层，总收回 980ms，履历恢复 100%。
- Reduced Motion 展开及收回各 300ms，取消旋转、超调、缩放；保留短淡入与位置变化。
- 重复点击锁定过渡；Escape 收回档案袋；离开页面取消动画、RAF、事件与音效，重新进入回到 closed。

## 刷新与首页按钮

仅 Cover 文档加载时播放 Opening。Choose、About、Archive、Other 和四个项目深链直接加载及刷新自身，保留各自已有入场效果。首页打开按钮从源画布 y=705 上移至 y=667，文案不变。

## 验收方法

`tools/verify-student-work.mjs` 检查真实鼠标命中、触屏、键盘、逐阶段图层和透明度、重复操作、过渡中离开、窗口变化及 Reduced Motion；对原项目按钮的位置、字体、颜色、字号、文字、z-index 和透明度做基线比较，并确认它仍可点击且继续经过 Choose 自动打开 Content。

尺寸：1568×900、1920×1080、1440×900、1366×768、1024×768、844×390；另验竖屏旋转提示。浏览器为 Edge 自动化模拟，未声称实体 iPad/iPhone 验证。

结果见 `student-work-verification.json`；原有入场、开屏、全场景、触屏、翻书和媒体使用对应 verify 脚本回归。16 个交互音效尚未提供，当前静默回退；傩场视频音乐独立保留。
